// Bu yukleme gorunumu, reports new sayfasi hazir olana kadar iskelet deneyimi sunar.

import { RouteLoadingFrame } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoadingFrame />;
}
